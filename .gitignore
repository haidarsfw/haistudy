# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# local DB backups (contain PII — never publish)
/backups/

# typescript
*.tsbuildinfo
next-env.d.ts
.github/mcp.json

# internal — kept locally, never published to the public repo
/CLAUDE.md
/AGENTS.md
/DISK-IO-CHANGES.md
/FREE-TIER-GUARDRAILS.md
/logobinus.png
/cowork/
/LAPORAN-PELAJARIN.md
/UX-PELAJARIN.md
/pelajarin-crawl-*.tgz

# impeccable design tooling — local dev tool, reinstall via `npx impeccable install`
/.claude/skills/impeccable/
# local Claude Code guardrails — hooks/subagents/local settings (public repo: keep local)
/.claude/hooks/
/.claude/agents/
/.claude/settings.local.json
# impeccable design context — kept local (public repo); reinstall/regenerate anytime
/PRODUCT.md
/DESIGN.md
/.impeccable/

# agent skills + tooling mirrors — kept local (public repo); regenerate via `npx skills add`
/.agents/
/.claude/skills/
/skills-lock.json
/.github/

# curated user/student data for the revamp (PII — never on public repo)
*.local.json
/testimonials*.json

# stale internal docs / scratch — stay out even if recreated
/*BROADCAST*.md
/MARKETING-*.md
/LAUNCH-CHECKLIST.md
/TEST-CHECKLIST-*.md
/TESTING.md
/r11-*.png
/r11b-*.png
# feedback / form-drive internal docs (per-exam, kept local, never published)
/Evaluasi-*.md
/Panduan-*.md
/Template-*.md

# CPU optimization changelog (local reference only)
CPU-OPTIMIZATION-CHANGELOG.md

testsprite_tests/tmp/config.json

# brand assets WIP — local only until build
/brand.local/
graphify-out/
